=- User: id, firstName, lastName, login, password
=- Cinema: cinema_ID, manager_ID, name
=- Room: room_id, name, rowsNumber, seatsInRowNumber, cinemaId
=- Seat: id, roomId, position, name, type
=- Movie: movies_ID, length, ageRestriction, cinema_ID, name, description
=- MovieCategory: id, name, description, cinemaId
=- Schedule: id, datetime, movieId, roomId, openSale, closeSale, seatLeft
=- Purchase: id, datetime, amount, paymentMethod, currency, scheduleId
=- Ticket: id, purchaseId, seatId, ticketTypeId
=- TicketType: id, name, price, cinemaId

--position in seat???
--ticket type cinema id for what?
--purchase amount float - binary, number - digitals
-- schedule open close sale why not date
--movie category should have movie_id, for what cinema_id
--unique login
--movie category should be in movie, instead of description
--dont need description, cinema in movie category